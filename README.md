# Family Budget Manager DBServer v4.2.0

Private persistence service. It stores validated, checksummed SQLite snapshots in Turso. Do not expose `DB_SERVER_SECRET` publicly. Deploy this service before the WebServer.
